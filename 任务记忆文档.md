# Serenity 任务记忆文档

更新时间：2026-06-12

## 当前目标

Serenity 项目已完成前后端分离、数据库设计、自审优化、部署手册、CI、本地预检和 Docker Compose 烟测。当前进入下一环节：准备 GitHub 首次上传，并为服务器拉取部署保留明确、可重复的操作路径。

## 已完成事项

- 项目结构已整理为 `backend/`、`frontend/`、`docs/`、`scripts/`、`.github/workflows/`。
- 后端为 FastAPI + SQLAlchemy + Celery + PostgreSQL + Redis 架构。
- 前端为 Vue + Vite + TypeScript 运维看板，生产镜像使用 nginx。
- 已生成数据库设计文档：`docs/database-design.md`。
- 已生成自审报告：`docs/self-audit.md`。
- 已生成傻瓜式部署手册：`docs/傻瓜式部署手册.md`。
- 已生成上线变量卡：`docs/上线变量卡.md`。
- 已新增本地预检脚本：`scripts/verify-local.ps1`。
- 已新增 Compose 运行时烟测脚本：`scripts/smoke-compose.ps1`。
- 已新增 GitHub Actions：`.github/workflows/ci.yml`。
- 已将 `ADMIN_PASSWORD`、`JWT_SECRET`、`POSTGRES_PASSWORD` 设为 Docker Compose 必填项。
- 已支持国内构建镜像源：Docker 基础镜像、APT、PyPI、npm registry 均可通过 `.env` 或环境变量切换。

## 已验证结果

- 后端测试通过：`python -m pytest -q`。
- Python 编译检查通过：`python -m compileall -q app`。
- 前端类型检查通过：`npm run typecheck`。
- 前端生产构建通过：`npm run build`。
- Docker Compose 配置展开通过：`docker compose config`。
- Docker 镜像构建通过：`scripts/verify-local.ps1 -DockerBuild`。
- Compose 运行时烟测通过：`scripts/smoke-compose.ps1`。
- 敏感信息扫描通过：未发现常见真实 API key/token 形态。
- 目录清理通过：无 `.env`、`node_modules`、`dist`、`__pycache__`、`.pytest_cache`。

## 重要约束

- 不要提交 `.env`、真实 API key、webhook secret、COS secret、数据库文件或缓存目录。
- 服务器部署前必须生成并填写 `ADMIN_PASSWORD`、`JWT_SECRET`、`POSTGRES_PASSWORD`。
- 生产域名按 `serenity.zenithy.art` 规划。
- Caddy 网关按现有 `hiremate` 的 `shared_gateway` 网络模式接入。
- GitHub 默认账号按 `YiyuZh`，仓库名按 `serenity`。
- 当前项目目录尚未初始化 Git；下一步可以执行 `git init`、首个 commit 和 push。

## 下一步

1. 运行提交前预检：`powershell -ExecutionPolicy Bypass -File .\scripts\verify-local.ps1 -DockerBuild`。
2. 初始化 Git 仓库并提交首版代码。
3. 在 GitHub 创建公开仓库 `YiyuZh/serenity`。
4. 推送 `main` 分支到 GitHub。
5. 在服务器 `/opt/apps/serenity` 拉取仓库。
6. 创建服务器 `.env` 并填入真实密钥。
7. 执行 `docker compose up -d --build`。
8. 更新 Caddyfile，接入 `serenity.zenithy.art`。
9. 验证 `https://serenity.zenithy.art/api/health` 和前端登录页。
