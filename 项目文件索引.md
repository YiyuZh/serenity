# Serenity 项目文件索引

更新时间：2026-06-12

## 根目录

| 路径 | 说明 |
|---|---|
| `.dockerignore` | Docker build context 忽略规则，避免密钥、缓存、构建产物进入镜像上下文。 |
| `.env.example` | 服务器 `.env` 模板，只保留变量名和占位，不保存真实密钥。 |
| `.github/workflows/ci.yml` | GitHub Actions CI，执行后端测试、前端校验、Compose 配置和敏感信息扫描。 |
| `.gitignore` | Git 忽略规则。 |
| `docker-compose.yml` | PostgreSQL、Redis、backend、worker、beat、frontend 的生产/预览编排。 |
| `README.md` | 项目入口说明、本地验证和部署入口。 |
| `Serenity_X_Realtime_Sync_PRD_v0.1.md` | Serenity X 同步服务 PRD。 |
| `任务记忆文档.md` | 当前任务状态、验证结论和下一步记忆。 |
| `项目文件索引.md` | 人类可读文件索引。 |
| `项目文件索引.json` | 机器可读文件索引。 |

## backend

| 路径 | 说明 |
|---|---|
| `backend/Dockerfile` | 后端生产镜像，非 root 用户运行，支持国内镜像源参数。 |
| `backend/pyproject.toml` | Python 项目配置和测试依赖声明。 |
| `backend/requirements.txt` | 后端运行时依赖。 |
| `backend/requirements-dev.txt` | 后端测试依赖入口。 |
| `backend/app/config.py` | 环境变量配置和生产安全校验。 |
| `backend/app/main.py` | FastAPI 应用创建和路由注册。 |
| `backend/app/cli.py` | `init-db` 运维命令。 |
| `backend/app/api/` | HTTP API、认证依赖和响应 DTO。 |
| `backend/app/application/` | 同步、账号、看板、帖子操作等应用服务。 |
| `backend/app/domain/` | 生命周期状态、帖子类型、任务类型等领域常量。 |
| `backend/app/infrastructure/` | 数据库、模型、安全、外部服务客户端边界。 |
| `backend/app/worker/` | Celery app、beat 调度和任务入口。 |
| `backend/migrations/001_initial.sql` | PostgreSQL 初始建表 SQL。 |
| `backend/migrations/001_initial_down.sql` | 初始迁移回滚 SQL。 |
| `backend/tests/` | 后端 API、数据库设计、同步链路、安全校验、worker 测试。 |

## frontend

| 路径 | 说明 |
|---|---|
| `frontend/Dockerfile` | Vue 构建 + nginx 生产镜像，支持国内 npm registry 和基础镜像参数。 |
| `frontend/nginx.conf` | 前端静态服务、`/api` 代理、安全响应头和 Vue history fallback。 |
| `frontend/package.json` | 前端脚本和依赖，包含 `typecheck`、`build`。 |
| `frontend/package-lock.json` | npm 锁文件。 |
| `frontend/tsconfig.json` | TypeScript 配置。 |
| `frontend/vite.config.ts` | Vite 配置和本地 `/api` 代理。 |
| `frontend/src/api/client.ts` | 前端 API client 和 token 存储。 |
| `frontend/src/router/index.ts` | Vue Router 和登录拦截。 |
| `frontend/src/views/` | 登录页、看板页、帖子详情页。 |
| `frontend/src/env.d.ts` | Vite 和 `.vue` 模块类型声明。 |

## docs

| 路径 | 说明 |
|---|---|
| `docs/database-design.md` | 数据库设计、ER 图、字段、约束、索引、状态机和自检。 |
| `docs/self-audit.md` | 多轮自审结果、修复项、验证结果和风险说明。 |
| `docs/上线变量卡.md` | GitHub、服务器、Docker、Caddy、`.env` 变量卡。 |
| `docs/傻瓜式部署手册.md` | 从本地检查到 GitHub、服务器拉取、Caddy 上线、回滚的操作手册。 |
| `docs/下一环节执行清单.md` | GitHub 首次上传和服务器部署的下一步执行清单。 |

## scripts

| 路径 | 说明 |
|---|---|
| `scripts/verify-local.ps1` | 本地一键预检，可选 `-DockerBuild`。 |
| `scripts/smoke-compose.ps1` | 临时 Compose 运行时烟测，自动清理临时容器和卷。 |
